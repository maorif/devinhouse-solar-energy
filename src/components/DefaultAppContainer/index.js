import React from "react";

import { AppContainer } from "./styles";

function DefaultAppContainer({children}) {

    return <AppContainer>{children}</AppContainer>
};

export default DefaultAppContainer;